image: python:latest
before_script:
  - python --version

stages:
  - alldoc
pages:
  stage: alldoc
  script:
    - pwd
    - ls ./
    - cd docs
    - make html
    - cd ..
    - mv docs/build/html public/
  artifacts:
    paths:
      - public
  only:
    - master
